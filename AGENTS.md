# Workspace Rules (Tools Hub)

## 1. การส่งมอบงานและ Git Workflow (สำคัญมาก)
- **ทำเสร็จแล้วต้องอัพเดตขึ้น Git เสมอ**: เมื่อทำงานเสร็จสิ้น หรือปรับปรุงโค้ด/ฟีเจอร์ตามที่ผู้ใช้มอบหมายเรียบร้อยแล้ว ให้ทำการ `git add`, `git commit` ด้วยข้อความที่สื่อความหมายชัดเจน และ `git push` ขึ้น repository ทันที โดยไม่ต้องรอให้ผู้ใช้เตือน

## 2. ภาษาและข้อความ
- ใช้ภาษาไทย สั้น กระชับ ตรงประเด็น ไม่เยิ่นเย้อ

## 3. ดีไซน์และธีม
- ธีมขาว (Clean White) เป็นค่าเริ่มต้น ควบคู่กับโหมดมืด (Dark Mode)
- หน้าหลักต้องใช้การจัดวางสไตล์ตารางเมนู POS (กล่องสี่เหลี่ยมจัตุรัส กดง่าย ไอคอนเด่น สัมผัสเด้งไว)
